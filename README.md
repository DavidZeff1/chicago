# Chicago Neighborhood Comparison App
Interactive app to compare Chicago's 77 community areas across quality-of-life metrics.

## Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
